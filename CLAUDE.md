# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This repository is currently empty. This file is a placeholder — update it as the project takes shape.

## To fill in as the project develops

- **Commands**: build, lint, test (including how to run a single test), and run/dev commands.
- **Architecture**: high-level structure and how major components fit together, once there's enough code that this isn't obvious from a directory listing.
